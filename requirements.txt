# Bibliotecas necessárias para o bot
# Adicionado pip explicitamente para garantir que a última versão seja usada para instalar as dependências
pip
flask               # Framework web para o servidor Flask (se estiver usando)
python-dotenv       # Para carregar variáveis de ambiente de um arquivo .env (uso local)
sqlalchemy          # ORM para interação com o banco de dados SQLite
pydantic>=2.0       # Validação de dados (usado por algumas bibliotecas, como OpenAI)
openai              # Cliente para a API da OpenAI (se estiver usando)

# Removendo a versão fixa para permitir a instalação de uma versão mais recente
# que seja compatível com um APScheduler mais novo (necessário para Python 3.11)
python-telegram-bot # Permitir a instalação da versão mais recente (20.x ou superior)

# A dependência 'telegram' geralmente é instalada automaticamente por python-telegram-bot
# Mas pode ser mantida se houver alguma dependência direta no seu código.
# telegram

pycoingecko         # Cliente para a API da CoinGecko

# Removendo a versão fixa de APScheduler e pytz.
# pip agora resolverá as dependências com base na versão de python-telegram-bot instalada.
# A versão mais recente de python-telegram-bot (20.x) dependerá de um APScheduler compatível com Python 3.11.
apscheduler
pytz
